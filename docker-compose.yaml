version: '3.8'
services:
  cv:
    build:
      context: ./
      target: runner
    volumes:
      - .:/app
    command: npm run start
    ports:
      - "3010:3000"
    environment:
      NODE_ENV: production
